export const COMMAND = [
  "Create component",
  "Create component with state",
  "Create component with style",
  "Create component with state and style",
];

export const IS_WITH_STATE: ITemplate = { isWithState: true };
export const IS_WITH_STYLE: ITemplate = { isWithStyle: true };
export const IS_WITH_STATE_STYLE: ITemplate = {
  isWithState: true,
  isWithStyle: true,
};
